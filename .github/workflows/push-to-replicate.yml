name: Push to Replicate

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  push:
    runs-on: 16-core-runner
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          sudo rm -rf /usr/share/miniconda
          sudo rm -rf /usr/local/share/chromium
          sudo rm -rf /usr/local/share/powershell
          sudo rm -rf /opt/microsoft
          sudo rm -rf /opt/pipx
          sudo rm -rf /opt/google
          sudo rm -rf /usr/lib/google-cloud-sdk
          sudo rm -rf /usr/lib/jvm
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' 'php.*' azure-cli google-chrome-stable firefox powershell mono-devel --fix-missing || true
          sudo apt-get autoremove -y || true
          sudo apt-get clean
          sudo docker system prune -af
          echo "Available disk space:"
          df -h /

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Debug - Show predict.py imports
        run: |
          echo "=== First 15 lines of predict.py ==="
          head -15 predict.py
          echo "=== Checking for FluxPipeline ==="
          grep -n "FluxPipeline" predict.py || echo "FluxPipeline NOT found (good!)"
          grep -n "AutoPipeline" predict.py || echo "AutoPipeline NOT found"

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Setup Cog
        uses: replicate/setup-cog@v2
        with:
          token: ${{ secrets.REPLICATE_API_TOKEN }}

      - name: Remove old images
        run: |
          docker rmi r8.im/ahmetsaid/pbr-texture-gen 2>/dev/null || true
          docker system prune -af

      - name: Build with Cog
        run: cog build --no-cache -t r8.im/ahmetsaid/pbr-texture-gen

      - name: Verify built image
        run: |
          echo "=== Checking predict.py inside Docker image ==="
          docker run --rm --entrypoint cat r8.im/ahmetsaid/pbr-texture-gen /src/predict.py | head -15

      - name: Login to Replicate Registry
        run: echo ${{ secrets.REPLICATE_API_TOKEN }} | docker login r8.im -u ahmetsaid --password-stdin

      - name: Push to Replicate
        run: docker push r8.im/ahmetsaid/pbr-texture-gen
